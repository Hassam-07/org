export interface Todo {
  // id?: number;
  // name: string;
  // complete?: boolean;
  // isLoading?: boolean;
  // pinned?: boolean;
  // editing?: boolean;
  id: number;
  name: string;
  complete: boolean;
  pinned: boolean;
}

// let todos: Todo[] = []
